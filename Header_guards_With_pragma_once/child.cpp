// The order of the includes can be arbitrary with either include guard: '#pragma once' or 'ifndef'.
// The included header files will be expanded by the preprocessor only once.
#include "grandparent.h"
#include "parent.h"

int main() {
    return 0;
}

